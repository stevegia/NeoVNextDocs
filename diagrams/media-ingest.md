# Media Ingest Pipeline

> Auto-generated by `scripts/trace_media_ingest.py` — do not edit manually.

## 1. Ingest Sequence Diagram

```mermaid
sequenceDiagram
    participant U as User
    participant FE as Frontend<br/>(Electron)
    participant MC as MediaController
    participant SS as MediaScanService
    participant FF as ffprobe
    participant MR as MediaRepository
    participant DB as SQLite<br/>(media_assets)
    participant FS as File System

    Note over U,MC: Path A — Manual scan trigger

    U->>FE: Click "Scan Directory" / enter path
    FE->>MC: POST /api/media/scan {path: "D:\\Videos"}
    MC->>SS: ScanAsync(path)

    Note over SS,DB: Phase B — Discovery & metadata extraction

    SS->>FS: Directory.EnumerateFiles(path, *.mp4|*.mov|*.avi|...)
    FS-->>SS: List of file paths

    loop For each media file
        SS->>MR: GetByFilePathAsync(filePath)
        MR-->>SS: null (new) or existing asset

        alt New file
            SS->>FF: ffprobe -print_format json -show_streams {filePath}
            FF-->>SS: {streams: [{width, height, duration, codec_name}]}
            SS->>SS: Extract metadata (mime, duration, resolution, codec)
            SS->>MR: InsertAsync(MediaAsset{...})
            MR->>DB: INSERT INTO media_assets
        else Already indexed
            SS->>SS: Skip (or update if file modified)
        end
    end

    SS-->>MC: {scanned: N, added: M, errors: E}
    MC-->>FE: 200 OK {scanned, added, errors}

    Note over FE,FS: Phase C — Catalog display & streaming

    FE->>MC: GET /api/media?query=&sort=created_at&page=1
    MC->>MR: SearchAsync(...)
    MR->>DB: SELECT ... FROM media_assets WHERE ... ORDER BY ... LIMIT ...
    DB-->>MR: rows
    MR-->>MC: (items, totalCount)
    MC-->>FE: {items: [...], page, pageSize, totalCount, totalPages}

    FE->>FE: Render MediaCard grid (catalog)

    U->>FE: Click media card
    FE->>MC: GET /api/media/{mediaId}
    MC->>MR: GetByIdAsync(mediaId)
    MR-->>MC: MediaAsset
    MC-->>FE: MediaAsset JSON

    FE->>MC: GET /api/media/{mediaId}/stream?token=...
    MC->>FS: PhysicalFile(asset.FilePath, contentType, enableRangeProcessing: true)
    FS-->>FE: Binary stream (HTTP Range supported)
    FE->>FE: Render in VideoPlayer / <img>
```

## 2. Data Model

```mermaid
erDiagram
    media_assets {
        TEXT media_id PK "UUID"
        TEXT file_name "e.g. interview.mp4"
        TEXT file_path "Absolute path on disk"
        INTEGER file_size_bytes
        TEXT mime_type "video/mp4, image/png, etc."
        INTEGER duration_seconds "null for images"
        INTEGER width
        INTEGER height
        TEXT codec "h264, hevc, vp9, etc."
        TEXT created_at
        TEXT updated_at
    }

    processing_jobs {
        TEXT job_id PK "UUID"
        TEXT media_id FK
        TEXT job_type "thumbnails, transcription, comfyui, etc."
        TEXT status "queued, dispatched, processing, completed, failed"
        TEXT worker_url
        TEXT error
        TEXT created_at
        TEXT updated_at
    }

    job_artifacts {
        TEXT artifact_id PK "UUID"
        TEXT job_id FK
        TEXT artifact_type "generated_0, transcript_json, etc."
        TEXT file_path "Absolute path to .bin file"
        INTEGER file_size_bytes
        TEXT storage_domain "generated, transcripts, thumbnails, etc."
        INTEGER sync_eligible
        TEXT created_at
    }

    pipeline_jobs {
        TEXT pipeline_job_id PK "UUID"
        TEXT media_id FK
        TEXT pipeline_type "cast_extraction, full_analysis, etc."
        TEXT status "queued, running, completed, failed"
        TEXT created_at
    }

    pipeline_child_jobs {
        TEXT child_job_id PK "UUID"
        TEXT pipeline_job_id FK
        TEXT job_id FK "references job_queue"
        INTEGER step_order
        TEXT handler_name
    }

    persons {
        TEXT person_id PK "UUID"
        TEXT name
        TEXT created_at
    }

    media_assets ||--o{ processing_jobs : "has jobs"
    processing_jobs ||--o{ job_artifacts : "produces artifacts"
    media_assets ||--o{ pipeline_jobs : "has pipelines"
    pipeline_jobs ||--o{ pipeline_child_jobs : "orchestrates"
```

## 3. File Path Translation (Docker)

When the backend runs in Docker, Windows drive paths need translation.

```mermaid
flowchart LR
    A["User enters:<br/>D:\Videos\clip.mp4"] --> B["ScanPathTranslation<br/>DriveMap__D = /host/d"]
    B --> C["Translated:<br/>/host/d/Videos/clip.mp4"]
    C --> D["Docker volume:<br/>D:/ -> /host/d:ro"]
    D --> E["ffprobe reads file at<br/>/host/d/Videos/clip.mp4"]

    style A fill:#263238,stroke:#78909c,color:#b0bec5
    style C fill:#1a3a4a,stroke:#00bcd4,color:#e0f7fa
    style E fill:#1b5e20,stroke:#4caf50,color:#c8e6c9
```

## 4. Streaming Architecture

| Feature | Implementation |
|---|---|
| Content-Type | `asset.MimeType` from DB (set during ffprobe scan) |
| Range Requests | `PhysicalFile(..., enableRangeProcessing: true)` |
| Auth | Bearer token via `Authorization` header or `?token=` query param |
| Thumbnail | Uses same `/stream` endpoint (full file); dedicated thumbnail pipeline planned |
| Formats | mp4, mov, avi, mkv, webm, png, jpg, gif, webp, wav, mp3 |
